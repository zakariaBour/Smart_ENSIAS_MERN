import { SearchFlterPipe } from './search-flter.pipe';

describe('SearchFlterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFlterPipe();
    expect(pipe).toBeTruthy();
  });
});
