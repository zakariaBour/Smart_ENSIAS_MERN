import { SearchHolidaysPipe } from './search-holidays.pipe';

describe('SearchHolidaysPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHolidaysPipe();
    expect(pipe).toBeTruthy();
  });
});
