import { SearchTeachersPipe } from './search-teachers.pipe';

describe('SearchTeachersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTeachersPipe();
    expect(pipe).toBeTruthy();
  });
});
